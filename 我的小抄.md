# 我的 GitHub 小抄 📝

## 常用三连（每次改完都要做）
git add .              # 装进篮子
git commit -m "说明"    # 存档
git push               # 上传到 GitHub

## 其他常用
git clone 网址          # 下载仓库
git pull               # 拉取最新
git status             # 看看现在的状态

## 分支 + PR 流程
git checkout -b 名字          # 开一条小路（分支）
git push -u origin 名字       # 推小路
gh pr create --title "标题" --body "说明"   # 发 PR
gh pr view                   # 看 PR
gh pr merge --merge          # 合并 PR

## 小技巧
cd 路径                # 走进文件夹（开新终端先做这个）
gh auth setup-git      # 让 git 认识 gh（忘了身份就试这个）
